Array :- 

1. Array is an object in java use to store Homogenous (same type of data/elements/values.

2. we need to define the size to create an array object and each memory cells recognized by the integer values staring from 0 and ends at (arrobjRefVar.length-1).where length is a default non static variable present within any array object.

3. we can create the array in different  ways :- 
  (I) :- Declaration and initialization
  
  D.T [] arrRefVar = {v1,v2,v3,v4.....};
  
  (II):- using new keyword 
  
  D.T[] arrRefVar = new D.T[size];
  
  
  
 we have two different set of classification :-
 [I]. Based on the DataType 
    -> Primitive Array
    -> Non Primitive Array
    
 [II]. Based on the arrangement  of element 
     -> 1-D  Array
     -> 2-D Array
     
     
     
     
     
     
============================================



Questions based on Array :- 

1. WAP to  create an array of character with the given size by the user and count the number of vowels present in them.
2. WAP to create an array of character with the given size by the user and count the number of digits , number of alphabet and number of special character and display them.
3. WAP to create an array of integer and display the element which are prime.
4. WAP to create an array of integer and display the maximum and minimum element from an array.
5. Distinct element find and display in another array.where array is [5,6,5,7,6,3] -> [5,6,7,3]
6. WAP to find a new array with distinct odd element.
7. WAP to find a new array with distinct even element.
8. WAP to find the occurrences of each and every elements of a given array.
9. WAP to sort an array using the bubble sort. [4,1,6,3,2]











